apiVersion: apps/v1
kind: Deployment
metadata:
  name: cheung-may1
spec:
  template:
    spec:
      containers:
        - name: cheung-may1
          resources:
            requests:
              memory: 128Mi
              cpu: 0.1
            limits:
              memory: 512Mi
              cpu: 1.0
